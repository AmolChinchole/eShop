# Backend Environment Variables (Render)
# Copy to .env locally; set these in Render dashboard under Environment.

MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/<dbName>?retryWrites=true&w=majority
JWT_SECRET=change_this_secret
STRIPE_SECRET_KEY=sk_test_yourStripeKeyHere
# Optional: If using email features
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
